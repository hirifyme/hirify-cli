# Приёмка: Windows открывает полный OAuth URL из Hirify CLI

- Источник: жалоба пользователя во вкладке `pm2__mu4f7p7j` от 2026-09-16 22:15
- Среда: local - мутации отсутствуют

## Критерии

1. [ ] `npm test` завершается успешно и включает сценарий `Windows opens the complete OAuth URL without a command shell`.
2. [ ] Windows-кандидат запуска браузера не использует `cmd.exe`, `cmd /c` или другой командный интерпретатор.
3. [ ] Один аргумент Windows-кандидата содержит полный OAuth URL вместе с `response_type`, `client_id`, `redirect_uri` и `state`; символы `&` не удалены и не преобразованы в границы команд.
4. [ ] Негативная проверка: временная возвратная замена Windows-кандидата на `cmd /c start` заставляет регрессионную проверку падать; после проверки дерево возвращено на пин.
5. [ ] `npm pack --dry-run` включает новый модуль запуска браузера, а версия пакета равна `0.4.7`.
